{
  "name": "budgetability-mcp-supabase",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server for the LIVE Budgetability app — operates on the production Supabase backend as your user (RLS enforced).",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "budgetability-mcp-supabase": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@supabase/supabase-js": "^2.47.0",
    "zod": "^3.24.1",
    "zod-to-json-schema": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
